{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn wsrun --stages build",
    "watch": "yarn wsrun watch"
  },
  "dependencies": {
    "wsrun": "^5.2.1"
  }
}
